service : {
  balanceOf: (principal) -> (record {
                               balance: nat;
                               symbol: text;
                             }) query;
  payOut: () -> (text);
  transfer: (principal, nat) -> (text);
}
